import { IInterceptor, IInterceptorOptions } from "unmock-core/dist/interceptor";
declare class NodeInterceptor implements IInterceptor {
    readonly config: IInterceptorOptions;
    private mitm;
    constructor(config: IInterceptorOptions);
    disable(): void;
    private initialize;
}
export default NodeInterceptor;
//# sourceMappingURL=node-interceptor.d.ts.map