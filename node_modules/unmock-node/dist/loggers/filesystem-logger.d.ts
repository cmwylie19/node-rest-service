import { IListener, ISerializedRequest, ISerializedResponse } from "unmock-core";
export default class FSLogger implements IListener {
    private static toIndentedYaml;
    private readonly enabled;
    private targetFile?;
    constructor({ directory, filename, }: {
        directory?: string;
        filename?: string;
    });
    notify({ req, res, }: {
        req: ISerializedRequest;
        res?: ISerializedResponse;
    }): void;
}
//# sourceMappingURL=filesystem-logger.d.ts.map